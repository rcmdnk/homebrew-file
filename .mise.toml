[env]
_.python.venv = ".venv"

[settings]
experimental = true

[settings.python]
uv_venv_auto = true

[hooks]
enter = [
  "mise trust >/dev/null 2>&1",
  "if [ ! -f $(git root)/.git/hooks/pre-commit ];then uv run pre-commit install >/dev/null;fi"
]
